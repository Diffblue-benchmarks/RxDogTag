buildCmd: ./gradlew assemble
testCmd: true
lineLength: 4096
skipVerification: true
cbmcArguments:
  # Do not generate coverage comments on generated tests.
  java-generate-no-comments: true
phases:
  -
  #Phase 0 PRETTY AND SIMPLE - Simple models, quick cover, pretty Tests, Do Not Test Private Method Directly
    timeout: 180
    cbmcArguments:
      # Use simple models containers (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 1500
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 1
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [ private ]
      # Instrument the entry point function only.
      cover-function-only: false
      # Instrument the file as opposed to the function only
      cover-only: file
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
  -
  #Phase 1 PRETTY AND COMPLEX - Standard models, longer run time, pretty tests, Do Not Test Private Method Directly
    timeout: 180
    cbmcArguments:
      # Use the standard models library
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: false
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 2
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [ private ]
      # Instrument the entry point function only.
      cover-function-only: false
      # instrument the file as opposed to the function only
      cover-only: file
  -
  #Phase 2 Simple and Private Directly
    timeout: 180
    cbmcArguments:
      # Use simple models containers (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 1500
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 1
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
      # If the logs contain output of the following then proceed to phase X
    nextPhase:
      # If the function is not analysable then do not pass onto the next phase
      not_analyzed: null
  -
  #Phase 3 Complex and Private Directly
    timeout: 180
    cbmcArguments:
      # Use the standard models library.
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 3000
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Bound the length of input strings.
      max-nondet-string-length: 30
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 3
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
  -
  #Phase 4 EXCEPTIONS
    timeout: 180
    cbmcArguments:
      # Use the standard models library.
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Bound the length of input strings.
      max-nondet-string-length: 30
      # Add constraint that strings are printable
      string-printable: false
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: true
      # Set an upper bound on loop unwinding.
  -
    #PHASE 5 Big numbers and less contraints on inputs 
    timeout: 300
    cbmcArguments:
      # Use the standard models library.
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: false
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 100
      # Bound the length of input strings.
      max-nondet-string-length: 100
      # Add constraint that strings are printable
      string-printable: false
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 6
units:
#Target TGF: 3.0
# - methods: 2
# - complexity: 6
# - construction: 1.0
# - dependency: 0.5
  - target:
    - dir: rxdogtag/build/classes/java/main
      file: com/uber/rxdogtag/RxDogTag$1.class
      sourceDir: rxdogtag/src/main/java
      sourceFile: com/uber/rxdogtag/RxDogTag.java
#Target TGF: 41.0
# - methods: 11
# - complexity: 78
# - construction: 4.33333333333
# - dependency: 4.33333333333
  - target:
    - dir: rxdogtag/build/classes/java/main
      file: com/uber/rxdogtag/DogTagSubscriber.class
      sourceDir: rxdogtag/src/main/java
      sourceFile: com/uber/rxdogtag/DogTagSubscriber.java
#Target TGF: 27.0
# - methods: 8
# - complexity: 64
# - construction: 3.33333333333
# - dependency: 3.33333333333
  - target:
    - dir: rxdogtag/build/classes/java/main
      file: com/uber/rxdogtag/DogTagCompletableObserver.class
      sourceDir: rxdogtag/src/main/java
      sourceFile: com/uber/rxdogtag/DogTagCompletableObserver.java
#Target TGF: 35.0
# - methods: 9
# - complexity: 66
# - construction: 3.33333333333
# - dependency: 3.83333333333
  - target:
    - dir: rxdogtag/build/classes/java/main
      file: com/uber/rxdogtag/DogTagSingleObserver.class
      sourceDir: rxdogtag/src/main/java
      sourceFile: com/uber/rxdogtag/DogTagSingleObserver.java
#Target TGF: 41.0
# - methods: 11
# - complexity: 78
# - construction: 4.33333333333
# - dependency: 4.33333333333
  - target:
    - dir: rxdogtag/build/classes/java/main
      file: com/uber/rxdogtag/DogTagMaybeObserver.class
      sourceDir: rxdogtag/src/main/java
      sourceFile: com/uber/rxdogtag/DogTagMaybeObserver.java
#Target TGF: 41.0
# - methods: 11
# - complexity: 78
# - construction: 4.33333333333
# - dependency: 4.33333333333
  - target:
    - dir: rxdogtag/build/classes/java/main
      file: com/uber/rxdogtag/DogTagObserver.class
      sourceDir: rxdogtag/src/main/java
      sourceFile: com/uber/rxdogtag/DogTagObserver.java
#Target TGF: 37.1369047619
# - methods: 18
# - complexity: 261
# - construction: 3.66666666667
# - dependency: 1.51785714286
  - target:
    - dir: rxdogtag/build/classes/java/main
      file: com/uber/rxdogtag/RxDogTag.class
      sourceDir: rxdogtag/src/main/java
      sourceFile: com/uber/rxdogtag/RxDogTag.java
#Target TGF: 2.96875
# - methods: 8
# - complexity: 95
# - construction: 0.3125
# - dependency: 2.5
  - target:
    - dir: rxdogtag-autodispose/build/classes/java/main
      file: com/uber/rxdogtag/autodispose/AutoDisposeObserverHandler.class
      sourceDir: rxdogtag-autodispose/src/main/java
      sourceFile: com/uber/rxdogtag/autodispose/AutoDisposeObserverHandler.java
dependenciesOnClasspath:
-
  source: classes
  classFile: com/uber/rxdogtag/RxDogTag$1.class
-
  source: classes
  classFile: com/uber/rxdogtag/RxDogTag$NonCheckingConsumer.class
-
  source: classes
  classFile: com/uber/rxdogtag/DogTagSubscriber.class
-
  source: classes
  classFile: com/uber/rxdogtag/DogTagCompletableObserver.class
-
  source: classes
  classFile: com/uber/rxdogtag/autodispose/AutoDisposeObserverHandler.class
-
  source: classes
  classFile: com/uber/rxdogtag/DogTagSingleObserver.class
-
  source: classes
  classFile: com/uber/rxdogtag/ObserverHandler.class
-
  source: classes
  classFile: com/uber/rxdogtag/RxDogTag$NonCheckingPredicate.class
-
  source: classes
  classFile: com/uber/rxdogtag/DogTagObserver.class
-
  source: classes
  classFile: com/uber/rxdogtag/RxDogTag.class
-
  source: classes
  classFile: com/uber/rxdogtag/DogTagMaybeObserver.class